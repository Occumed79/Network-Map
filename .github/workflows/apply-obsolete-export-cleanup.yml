name: Apply obsolete export cleanup

on:
  push:
    branches:
      - hardening/ui-ownership-consolidation-v2

permissions:
  contents: write

concurrency:
  group: obsolete-export-cleanup
  cancel-in-progress: true

jobs:
  apply-and-validate:
    if: ${{ !contains(github.event.head_commit.message || '', '[obsolete-export-removed]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Check out consolidation branch
        uses: actions/checkout@v4
        with:
          ref: hardening/ui-ownership-consolidation-v2
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable Corepack
        run: corepack enable

      - name: Remove obsolete export at source level
        working-directory: occu-med-map
        run: node scripts/apply-obsolete-export-removal.mjs

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck frontend
        run: pnpm --filter @workspace/occu-med-map typecheck

      - name: Run sidebar workspace smoke
        run: pnpm --filter @workspace/occu-med-map test:sidebar-workspace-hardening

      - name: Run general UI smoke
        run: pnpm --filter @workspace/occu-med-map test:general-ui-hardening

      - name: Run runtime ownership smoke
        run: pnpm --filter @workspace/occu-med-map test:runtime-ownership

      - name: Build frontend
        run: pnpm --filter @workspace/occu-med-map build

      - name: Commit validated source cleanup
        shell: bash
        run: |
          rm -f occu-med-map/scripts/apply-obsolete-export-removal.mjs
          rm -f .github/workflows/apply-obsolete-export-cleanup.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Remove obsolete Finder export at source level [obsolete-export-removed]"
          git push origin HEAD:hardening/ui-ownership-consolidation-v2
