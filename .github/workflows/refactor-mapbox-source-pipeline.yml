name: One-time Mapbox source pipeline refactor

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/refactor-mapbox-source-pipeline.yml
      - .github/scripts/finalize-mapbox-source-pipeline.mjs

permissions:
  contents: write

jobs:
  refactor:
    if: github.head_ref == 'hardening/unify-mapbox-source-pipeline' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Finalize source pipeline ownership
        run: node .github/scripts/finalize-mapbox-source-pipeline.mjs

      - name: Commit source finalization
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add occu-med-map/src occu-med-map/scripts
          git diff --cached --stat
          git commit -m "Remove competing dual-engine overlay writer"
          git push origin HEAD:${{ github.head_ref }}
